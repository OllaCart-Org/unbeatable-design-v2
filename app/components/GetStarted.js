import React from "react";

const GetStarted = ({ openModal }) => {
  return (
    <div className="get-started-btn" onClick={openModal}>
      Get Started
    </div>
  );
};

export default GetStarted;
